machine:
  php:
    #TODO: This breaks circle since this version isn't included. Figure out how to install php 5.3.29
    version: 5.3.29

test:
  override:
    # syntax check php files
    - find . -name "*.php" -type f -print0 | xargs -0 -I {} php -l {}
    - vendor/bin/phpunit tests
